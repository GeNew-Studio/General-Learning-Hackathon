{
  "name": "vibeblog",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:extension": "node scripts/sync-lesson-to-extension.js",
    "sync:workspace": "node scripts/sync-workspace.js",
    "check:machine": "node scripts/check-machine.js && node scripts/check-lesson.js && node scripts/check-journal.js",
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vite-plugin-monaco-editor": "^1.1.0"
  },
  "dependencies": {
    "monaco-editor": "^0.56.0"
  }
}
